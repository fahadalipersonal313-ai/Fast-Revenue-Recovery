streamlit>=1.33
pandas>=2.0
openpyxl>=3.1
APScheduler>=3.10
pydantic>=2.5
pytest>=7.4
# Encrypts per-tenant email credentials at rest (see src/crypto.py).
cryptography>=42.0
# Makes Python TLS use the OS trust store so HTTPS works behind antivirus /
# proxy SSL scanning (e.g. Avast Web Shield). Without it, API/IMAP calls can
# fail with CERTIFICATE_VERIFY_FAILED on some Windows machines.
truststore>=0.9
# Optional: only needed if you enable AI message improvement.
# Gemini is the default provider (free tier). GLM (Z.ai, free flash models, via
# its OpenAI-compatible REST endpoint over requests) and Anthropic are also
# supported.
google-genai>=0.3
anthropic>=0.40
requests>=2.31
# PDF rendering for the Invoice Generator page (pure Python, no system deps).
reportlab>=4.0
